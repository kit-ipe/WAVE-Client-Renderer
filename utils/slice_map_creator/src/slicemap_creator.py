#!/usr/bin/python
# -*- coding: UTF-8 -*-

class SliceMapCreator:
    def create(self):
        print self.config

    def __init__(self, config):
        # self.area_of_slice = config.area_of_slice
        # self.command = config.command
        # self.filter = config.filter
        # self.path_to_images = config.path_to_images
        # self.images_name_format = config.images_name_format
        # self.path_to_slicemaps = config.path_to_slicemaps
        # self.slicemaps_name_format = config.slicemaps_name_format
        # self.row_col = config.row_col
        # self.slicemap_size = config.slicemap_size
        # self.slicemaps_number = config.slicemaps_number
        # self.slices_range = config.slices_range

        self.config = config